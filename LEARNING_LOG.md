# AI Learning Log

> 每次会话结束时自动分析、追加。记录知识盲区和学习进度。

---

## 2026-03-04 | Week 1 Embedding 基础 | 会话 #1

### 本次覆盖内容
- [x] Embedding 概念：文本 → 高维向量
- [x] 余弦相似度：公式理解 + 手写实现
- [x] 欧氏距离 vs 余弦相似度的区别
- [x] 向量维度的含义（768 维、模型决定）
- [x] Embedding API 调用（Ollama 兼容 OpenAI 格式）
- [x] 分析相似度矩阵，发现模型对中文的局限

### 知识掌握评估

| 知识点 | 掌握度 | 说明 |
|--------|--------|------|
| Embedding 是什么 | ⭐⭐⭐ | 理解文本→向量的映射，知道向量值无法单独解读（分布式表示） |
| 余弦相似度公式 | ⭐⭐⭐ | 能手写实现，理解"看方向不看长度" |
| 欧氏距离 | ⭐⭐ | 知道概念和公式，但对"什么场景用欧氏"还不清晰 |
| 向量维度 | ⭐⭐ | 知道是超参数，但对"维度灾难"等深层问题未涉及 |
| 模型对中文的理解 | ⭐⭐⭐ | 通过实验发现 nomic-embed-text 捕捉句子结构而非语义，洞察力强 |
| API 调用 | ⭐⭐⭐ | HTTP 三要素清楚，能独立实现 |

### 发现的知识缺陷

1. **欧氏距离的适用场景**：知道余弦适合文本，但不清楚什么场景下欧氏距离更好（如图像特征？聚类？）
2. **向量值的含义**：提问了"每个数字代表什么"，目前接受了"分布式表示"的解释，但对 Transformer 如何学出这些值缺乏理解
3. **Tokenization**：完全未涉及。文本在变成向量之前如何被切分成 token？中文的分词策略？
4. **维度与效果的关系**：知道"不是越高越好"，但缺乏量化理解（在什么阈值下收益递减？）
5. **Python 语法**：来自 Java/JS 背景，对 `zip()`、列表推导式、`np.linalg.norm()` 等需要逐步适应

### 学习风格观察
- 善于提出"为什么"类问题，思考深度好
- 倾向于先理解概念再动手，需要鼓励"先写再理解"
- 能从实验数据中发现规律（如定义句结构 → 高相似度）

### 下次会话建议
- W2 开始时，先复习余弦相似度公式（确保不是短期记忆）
- 分块实验中自然引入 Tokenization 概念
- 鼓励先写代码再问问题，减少"消化时间"

---

## 2026-03-04 | Week 2 分块 + 模型选型（未开始） | 会话 #2

### 本次覆盖内容
- [ ] W1 余弦相似度公式回顾（未完成，用户直接结束）

### 知识掌握评估

| 知识点 | 掌握度 | 说明 |
|--------|--------|------|
| 余弦相似度（复习） | ❓ | 提出了回顾问题但用户未作答，无法评估是否留存 |

### 发现的知识缺陷
1. **W1 知识留存未验证**：余弦相似度公式的回顾问题已提出但未回答，无法确认是短期记忆还是真正掌握

### 学习风格观察
- 本次会话极短（1 轮交互），无有效观察
- 可能时间不够，下次需要更快进入正题

### 下次会话建议
- **仍需先做 W1 回顾**：余弦相似度公式 + "为什么只看方向" — 这个问题还没回答
- 回顾通过后立即进入 W2 分块策略，不要再拖延
- 考虑缩短回顾环节（如果用户答对第一题就直接跳过）

---

## 2026-03-11 | Week 2 分块 + 模型选型 | 会话 #3

### 本次覆盖内容
- [x] W1 回顾：余弦相似度看方向、不主要看长度
- [x] 三种分块策略：固定长度、按段落、滑动窗口
- [x] overlap 的作用与 `window_size - overlap` 步长
- [x] `search_chunks()` 数据流：query embedding、chunk embeddings、排序、Top-K
- [x] 结合实验输出分析不同分块策略的检索效果
- [x] 根据文档类型选择分块策略：说明文、聊天记录、代码文档
- [x] 暴露出 Python 语法与常见写法不熟的问题

### 知识掌握评估
| 知识点 | 掌握度 | 说明 |
|--------|--------|------|
| 余弦相似度核心直觉 | ⭐⭐⭐ | 能明确说出文本检索更关注向量方向而不是长度 |
| 固定长度 / 段落 / 滑窗三种分块原理 | ⭐⭐⭐ | 能解释各自机制、优缺点和适用场景 |
| overlap 的作用 | ⭐⭐⭐ | 能说明 overlap 保留边界上下文，过大则冗余增加 |
| `search_chunks()` 数据流 | ⭐⭐⭐ | 已能清楚区分 query embedding、chunks、chunk embeddings，并复述完整流程 |
| 根据文档类型选分块策略 | ⭐⭐ | 对说明文和聊天记录判断较好，但代码文档分块策略还需要进一步巩固 |
| Python 语法表达能力 | ⭐⭐ | 逻辑理解已到位，但在 `zip`、`enumerate`、列表组织等写法上仍不够熟练 |

### 发现的知识缺陷
1. **Python 语法熟练度不足**：已经能理解检索数据流，但一到代码层容易被 `zip()`、`enumerate()`、切片、排序写法卡住
2. **数据结构容易混淆**：一开始会把 `query_embedding` 与 `chunk_embeddings` 混在一起，需要继续通过小规模代码阅读强化
3. **实验结论表达不够完整**：理解已经到位，但在书面总结和面试化表达上还偏简略，需要继续训练“实验现象 -> 原因 -> 结论”三段式表达
4. **代码文档分块策略理解还不够稳**：已经意识到代码不同于自然语言，但“按结构分块优先于滑动窗口”这一点还需要进一步固化

### 学习风格观察
- 先问清数据流和关系后，理解速度明显提升
- 遇到 Python 语法会不自信，但概念层面的判断其实已经较准确
- 对“为什么这么设计”类问题反应较好，适合继续用代码阅读式学习而不是直接整段硬写
- 通过实际实验输出做结论分析，吸收效果明显好于单纯讲概念

### 下次会话建议
- W3 开始前，先用 5 分钟复习 `query embedding / chunk embeddings / Top-K` 的数据流
- 进入向量数据库时，继续沿用“主线概念 + 最小 Python 语法补丁”的节奏，不单独开纯 Python 学习线
- 强化代码文档分块与自然语言分块的区别，补上“按函数 / 类 / 模块切分”的工程直觉
- 开始训练实验结论表达，尽量把回答写成“现象、原因、结论”三段式

---

## 2026-03-11 | Week 2 分块 + 模型选型（收尾） | 会话 #4

### 本次覆盖内容
- [x] 补充完善 W2 思考题答案
- [x] 完成 Week 2 本周总结
- [x] 解释 `LEARNING_LOG.md` 的触发与记录机制
- [x] 识别并修正“结束总结未自动写入”的执行遗漏

### 知识掌握评估
| 知识点 | 掌握度 | 说明 |
|--------|--------|------|
| W2 实验结论总结 | ⭐⭐ | 已能说出按段落分块优于固定长度分块的核心原因，但表达还偏短 |
| 面试化表达 | ⭐⭐ | 能抓住核心结论，但需要继续补充“现象 -> 原因 -> 结论”的完整结构 |
| Learning Log 机制理解 | ⭐⭐⭐ | 已理解当前机制是模型识别关键词后手动追加，而非系统真 hook |

### 发现的知识缺陷
1. **结论表达仍偏简略**：能答对，但容易只说一句结论，缺少例子和因果展开
2. **机制预期与实现细节未完全对齐**：会自然预期“自动触发”，但当前项目实际还是半自动流程

### 学习风格观察
- 对流程机制类问题也会追问实现方式，不满足于“知道有这个功能”
- 在总结阶段会主动检查机制是否真的执行，说明对项目约定比较敏感

### 下次会话建议
- 进入 W3 前，先用 2 分钟复述一次 W2 的核心实验结论
- 继续训练把“答对”升级为“答完整”，每次回答尽量补上一个原因或例子
- W3 开始时直接从”为什么需要向量数据库”切入，不再回到 W2 代码细节

---

## 2026-03-18 | Week 3 向量数据库（Chroma） | 会话 #5

### 本次覆盖内容
- [x] W2 回顾：按段落分块 vs 固定长度分块的对比结论（现象→原因→结论三段式）
- [x] 暴力搜索的两个瓶颈：重复计算 embedding + O(n) 遍历
- [x] 向量索引的直觉：树结构 → 高维空间不适用 → HNSW “问路找餐厅”类比
- [x] ANN（近似最近邻）概念：用精度换速度
- [x] Chroma 核心概念：Client / Collection / Document 对应 MySQL 的 Database / Table / Row
- [x] Chroma 实战：创建 Collection、手动传 embedding 存储、语义检索、metadata 过滤、持久化存储
- [x] 对比 Chroma 和 W2 手写检索的结果
- [x] 分析 Chroma distances 和余弦相似度的关系（distance = 1 - cosine_similarity）
- [x] 发现 nomic-embed-text 对中文检索的局限（”RAG 系统有哪些组件”检索失准）
- [x] 完成 6 道思考题

### 知识掌握评估
| 知识点 | 掌握度 | 说明 |
|--------|--------|------|
| 向量数据库解决什么问题 | ⭐⭐⭐ | 能清楚说出预计算+持久化+索引加速三个层面 |
| Chroma 基本 API（add/query/metadata） | ⭐⭐⭐ | 6 个函数全部自己实现并跑通 |
| ANN 概念及 trade-off | ⭐⭐⭐ | 能说出牺牲精度换速度，理解 RAG 场景中可接受 |
| HNSW 原理 | ⭐⭐ | 理解”跳着走找邻居”的直觉，但对分层结构、参数（ef、M）等细节未深入 |
| distance 与 cosine_similarity 关系 | ⭐⭐ | 经引导后理解 distance = 1 - similarity，但最初未能自行推导 |
| 高维空间不适合传统树索引 | ⭐⭐ | 经引导后理解”没有全局排序”的核心原因，最初回答偏了（认为是返回数量的差异） |
| Metadata 过滤 | ⭐⭐ | 代码实现正确，但对 post-filtering 机制（先搜索再过滤）未主动意识到 |
| Chroma 返回结果的数据结构 | ⭐⭐⭐ | 遇到 KeyError 后自己修复，把 dict 转成元组列表 |
| W2 回顾表达 | ⭐⭐⭐ | 本次能用对比结构回答”段落保留语义 vs 固定长度切散语义”，比上次完整 |

### 发现的知识缺陷
1. **高维索引理解偏差**：最初认为传统树结构不适合向量检索是因为”查询结果是多个”，实际是因为高维空间无法定义全局排序。需要强化”维度”对算法设计的影响
2. **distance 与 similarity 的转换不敏感**：看到 distance 值时没有主动联想到和余弦相似度的关系，需要更多在不同度量之间做转换的练习
3. **ANN 精度损失的应用判断**：回答中说”用户可以从中选择”，但 RAG 中用户不直接看检索结果——对 RAG 数据流中各角色的边界还需要更清晰的认知
4. **post-filtering vs pre-filtering**：思考题中给出了合理的分析，但没有意识到 Chroma 实际用的是 post-filtering，说明对框架实现细节关注不够

### 学习风格观察
- 代码实现能力明显提升：6 个函数基本一次写对，遇到 Chroma dict 结构问题也能自己修复
- 概念推导上仍偏被动，需要引导才能建立新旧知识的关联（如 distance ↔ similarity）
- 对”课件准备”有明确预期，认为目录结构和实验文件属于导师职责——说明对学习流程有清晰的角色分工意识
- 对外部链接质量敏感，会主动验证链接有效性并反馈

### 下次会话建议
- W4 是第一阶段里程碑：串联 W1-W3 组装完整 RAG 管道，重点在于理解各组件如何协作
- 开始前用 2 分钟画一下 RAG 的数据流：文档 → 分块 → Embedding → 向量数据库 → 检索 → Prompt → LLM → 回答
- 继续强化 distance/similarity 等度量概念的直觉，在 W4 中自然带入
- Prompt 工程部分注意区分 System Prompt、Few-shot、Context Window 的作用和限制

---

## 2026-03-19 | Week 4 Prompt 基础 + RAG 管道 | 会话 #6

### 本次覆盖内容
- [x] W3 回顾：distance = 1 - cosine_similarity 转换
- [x] Chat API 与 Embedding API 的区别：messages 结构、role 的作用
- [x] temperature 参数对生成结果的影响（随机性 vs 确定性）
- [x] System Prompt 的作用：同一问题用不同 System Prompt 得到不同风格回答
- [x] Few-shot Prompting：zero-shot vs few-shot 的格式控制能力对比
- [x] Context Window 限制：估算 token 数，理解为什么不能把所有文档塞进去
- [x] 构建完整 RAG 管道：分块 → 向量化 → Chroma 存储 → 检索 → Prompt 构造 → LLM 生成
- [x] 对比实验：有无 RAG 的回答质量差异（幻觉 vs 忠实于文档）
- [x] 完成 5 道思考题

### 知识掌握评估
| 知识点 | 掌握度 | 说明 |
|--------|--------|------|
| Chat API messages 结构 | ⭐⭐⭐ | 理解 role 区分的必要性，能独立构造 messages |
| temperature 参数 | ⭐⭐⭐ | 理解随机采样机制，能判断 RAG 场景应设低温 |
| System Prompt | ⭐⭐⭐ | 通过实验直观感受到风格控制能力 |
| Few-shot Prompting | ⭐⭐⭐ | 理解用示例定义输出格式比文字描述更可靠 |
| Context Window 限制 | ⭐⭐⭐ | 能估算 token 数，理解 RAG 存在的必要性 |
| RAG 端到端管道 | ⭐⭐⭐ | 独立实现完整管道：分块→向量化→存储→检索→Prompt→LLM |
| RAG vs 直接问 LLM 的差异 | ⭐⭐⭐ | 通过对比实验清晰看到 RAG 减少幻觉、提高准确性 |
| System Prompt 与 User Prompt 优先级 | ⭐⭐ | 直觉正确（系统优先），但对 prompt injection 等边界情况未深入 |

### 发现的知识缺陷
1. **Prompt injection 安全性**：Q1 回答中认为"系统指令高于用户指令"，实际 LLM 对此没有硬保障，恶意用户输入可以绕过 System Prompt。对 prompt 安全的认知需要补强
2. **Few-shot 放置位置的权衡**：Q4 倾向全放 User Prompt，但未考虑固定模板放 System Prompt 的效率优势。对"什么内容放哪里"的工程决策还需更多实践
3. **检索质量与生成质量的关系**：Q5 答到了"garbage in, garbage out"，但对如何量化检索质量（Recall、Precision）还未涉及，这是 W8-W9 的重点

### 学习风格观察
- 代码实现速度明显提升，8 个函数基本一气呵成，中间只因超时卡了一次
- 会主动提问（如 temperature 导致输出不同、context_window_demo 不理解做什么），说明不盲目执行，有自己的节奏
- 对 RAG 管道中各组件的角色已经有清晰的全局视图
- 踩坑后能自己定位问题（超时 → 调整环境变量），排障能力在提升

### 下次会话建议
- 第一阶段里程碑验证：补上 PDF 文档读取，实现"对一份 PDF 文档问答"的端到端 RAG
- 验证完成后可直接进入 W5 混合检索（BM25 + 向量双路检索）
- W5 中自然引入检索质量评估的概念，为 W8-W9 做铺垫
- 后续 Prompt 安全性话题可在 W11 Agent 阶段深入讨论

---

## 2026-03-27 | Milestone 1 PDF 端到端 RAG 问答 | 会话 #7

### 本次覆盖内容
- [x] 完成 `common/pdf_reader.py` 的 PDF 文本提取能力
- [x] 选定 `rag_intro_zh.pdf` 作为第一阶段主验证资料
- [x] 跑通 PDF 版端到端 RAG：PDF 读取 → 分块 → Embedding → Chroma → 检索 → Prompt → LLM
- [x] 完成交互式问答与预设问题评估
- [x] 回答里程碑思考题，比较真实 PDF 与 W4 演示文本的差异

### 知识掌握评估
| 知识点 | 掌握度 | 说明 |
|--------|--------|------|
| PDF 文本读取与清洗 | ⭐⭐⭐ | 已能把真实 PDF 转成可用于检索的纯文本，并理解格式噪声会影响后续效果 |
| 真实文档版 RAG 闭环 | ⭐⭐⭐ | 已将 W1-W4 的能力串成可运行的完整问答链路 |
| 检索质量与文档质量关系 | ⭐⭐⭐ | 能明确指出真实 PDF 比硬编码文本噪声更高，对分块和检索要求更高 |
| 事实型问题 vs 综合型问题表现差异 | ⭐⭐ | 已观察到事实题更稳定，但对“为什么综合题更难”还可继续深化 |
| 优化优先级判断 | ⭐⭐ | 已倾向优先优化文本结构化处理与分块，但对多种检索策略的 trade-off 还未实测 |

### 发现的知识缺陷
1. **综合型问题的检索支撑仍不稳定**：已经能识别问题，但还没有量化分析是 `top_k`、分块还是向量模型导致的
2. **真实文档噪声处理经验仍偏少**：知道 PDF 比演示文本更难，但对标题识别、结构化清洗、去噪规则还缺少系统经验
3. **混合检索动机已具备，但证据还不够硬**：已经感受到纯向量检索在精确术语场景下的局限，下一步需要通过 W5 实验把直觉变成结论

### 学习风格观察
- 会主动把“实验能跑”继续追问到“为什么在真实文档里效果变化了”，说明开始从实现视角转向工程视角
- 在完成闭环后能够回看每个环节的瓶颈，而不是只停留在“跑通了”
- 对真实材料的可用性比较敏感，愿意接受比演示案例更脏的数据来验证系统边界

### 下次会话建议
- 正式进入 W5：混合检索（BM25 + 向量双路召回）
- 先回答一个核心问题：为什么纯向量检索在真实资料场景里不够稳定
- 做对比实验时，重点观察“术语/缩写/精确名词”查询与“语义描述型”查询的差异
- 开始建立检索效果比较意识，为 W8-W9 的评估体系打基础

---

## 2026-04-02 | 路线重构 + 课件策略澄清 | 会话 #8

### 本次覆盖内容
- [x] 分析一篇关于“Java 后端转 AI Agent 候选人”的帖子，区分合理批评与情绪化放大
- [x] 结合当前项目判断哪些观点值得吸收，哪些不应跟风调整
- [x] 重写 `README.md` 的 13 周总路线，强调“可靠 Agent 工程”而不是框架名词堆砌
- [x] 补强 W4/W5 课件中的上下文预算、Prompt Injection、失败模式与工程边界
- [x] 明确课件生成策略：当前阶段只规划大方向，不提前写死后续周课件；到对应阶段再结合学习日志生成针对性课件

### 知识掌握评估
| 知识点 | 掌握度 | 说明 |
|--------|--------|------|
| Agent 工程与 Demo 的区别 | ⭐⭐ | 已能识别“只会 Function Calling / ReAct 名词”并不等于会做可控 Agent，但还未进入亲手实现验证阶段 |
| 可靠 Agent 的关键约束 | ⭐⭐ | 已接受工具白名单、失败恢复、状态管理、人工审批等概念的重要性，但尚未通过代码闭环内化 |
| RAG 优化与 Agent 路线的先后顺序 | ⭐⭐⭐ | 明确当前项目不应因为外部帖子而跳过 RAG 优化阶段，路线判断较稳 |
| 课件与总路线的关系 | ⭐⭐⭐ | 已明确 README 负责方向，LEARNING_LOG 负责暴露薄弱点，课件应在对应阶段按需生成 |
| Prompt Injection 与非可信上下文意识 | ⭐⭐ | 已意识到这类安全问题需要前移补强，但还没有通过实验进一步验证细节 |

### 发现的知识缺陷
1. **Agent 工程的控制面仍停留在概念层**：已经知道需要状态管理、失败恢复、工具边界，但还没有亲手做过最小运行时闭环，属于“理解重要性但尚未实践”
2. **对课件生成时机的边界刚刚厘清**：此前存在“先把后续课件都补好”的倾向，说明对“路线规划”和“按学习状态生成课件”这两层职责还需要持续保持清晰
3. **安全与上下文管理尚未形成实验直觉**：虽然已经接受 Prompt Injection、上下文预算应前移，但还没有通过实际样例看到这些问题如何影响 RAG 输出

### 学习风格观察
- 能先质疑外部观点是否合理，而不是直接照单全收，说明开始具备工程判断意识
- 对“路线规划”和“具体课件生成”能主动区分层次，说明不再只关注内容多少，而开始关注教学策略本身
- 更倾向先把方向定准，再按阶段推进，不急于一次性补齐所有材料，这种节奏更适合长期学习项目

### 下次会话建议
- 回到当前主线，正式进入 W5 混合检索实验，不要继续被 Agent 话题打断节奏
- 在 W5 实验中重点观察“召回更稳”与“最终回答更好”之间的差异，为 W6/W8 铺路
- 后续生成新周课件时，严格先读最新 `LEARNING_LOG.md` 和 `COURSEWARE_LOG.md`，避免再次提前固化后续周内容

---

## 2026-04-03 | W4.5 补课：Prompt Injection + 上下文预算 | 会话 #9

### 本次覆盖内容
- [x] 判断教学大纲更新后是否需要大范围补课，确认只做 `W4.5` 最小补课而不回炉重学 W4
- [x] 通过苏格拉底式问答澄清 `Prompt Injection`、非可信上下文与 `System Prompt` 不是硬边界的关系
- [x] 区分 `没召回`、`召回了但排错了`、`上下文变脏了`、`生成层没用好证据` 四类故障
- [x] 理解 `top_k` 变大不等于回答更好，识别噪声、冲突信息和上下文预算稀释
- [x] 设计 `W4.5` 最小对照实验：固定文档/问题/检索流程，只改变 `System Prompt` 并先检查恶意 `chunk` 是否进入 `Top-K`
- [x] 明确 `prompt assembly` 需要区分“规则”和“证据”，不能把检索文本与系统规则混在一起
- [x] 明确本次课程边界：本课结束于 `W4.5`，`W5` 下次单独开课

### 知识掌握评估
| 知识点 | 掌握度 | 说明 |
|--------|--------|------|
| Prompt Injection 与非可信上下文 | ⭐⭐ | 已能说明检索文本不是可信指令，知道 `System Prompt` 只能降低风险而非提供硬保障 |
| 检索/排序/上下文污染/生成失败的分层诊断 | ⭐⭐ | 已能区分四类常见失败来源，但还未通过真实实验样例反复验证 |
| 上下文预算与 `top_k` trade-off | ⭐⭐ | 已理解 `top_k` 增大可能带来噪声和冲突，不再把“召回更多”直接等同于“答案更好” |
| 安全 `System Prompt` 约束设计 | ⭐⭐ | 已能写出“仅依据上下文回答、无证据拒答、忽略伪指令、不做脑补”的基础约束 |
| 单变量对照实验设计意识 | ⭐⭐⭐ | 已能明确固定文档、问题、`top_k`、分块和检索流程，只改变 `System Prompt` 来观察抗污染效果 |
| Prompt 组装中的规则/证据分层 | ⭐⭐ | 已意识到 `prompt assembly` 是风险入口，但还未落到代码级实现与验证 |

### 发现的知识缺陷
1. **Prompt Injection 仍停留在概念验证前**：已经能说清原理和实验设计，但还没有亲手跑出“恶意 `chunk` 进入 `Top-K` 后基础 Prompt 与加强 Prompt 的差异”
2. **`prompt assembly` 的代码级边界还未内化**：已经知道规则和证据要分层，但尚未检查当前实现到底如何拼接上下文、是否存在截断或过滤
3. **生成层“没用好证据”的判断还缺实证**：已经能口头区分它与检索/排序问题，但还没有拿真实失败样例验证这种归因

### 学习风格观察
- 会在推理链出现不严谨时立即质疑，例如主动指出“文档会分块，放在开头不一定天然更强”，说明开始关注实验设计的有效性而不只听结论
- 对课程边界非常敏感，明确提出“一课一结”，说明对学习节奏和教学颗粒度有清晰要求
- 在连续追问中能逐步修正概念，从模糊直觉收敛到较清晰的工程边界，适合用归因题和反例题继续推进

### 下次会话建议
- 下次单独开启 W5：混合检索，不再继续沿用本次补课节奏
- 开课前先用 2 分钟复述四类诊断：`没召回`、`排错了`、`上下文变脏了`、`生成层没用好证据`
- W5 实验里重点观察“召回更稳”和“最终回答更好”不是同一件事，避免把所有退化都归因到模型本身
- 如果进入 W5 前还想做最小验证，可单独补跑一次 `W4.5` 对照实验，但应视为新课的开场实验，而不是延长本次课程
